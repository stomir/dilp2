empty(llempty,llempty).
head(llabc,a).

head(llbc,b).
head(llab,a).

head(llc,c).
head(lla,a).
head(llb,b).

tail(llabc,llbc).

tail(llbc,llc).
tail(llab,llb).

tail(lla,llempty).
tail(llb,llempty).
tail(llc,llempty).
