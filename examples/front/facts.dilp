tail(llab,llb).
head(llab,a).

head(llabc,a).
tail(llabc,llbc).

head(llc,c).
tail(llc,llempty).

head(llb,b).
tail(llb,llempty).

head(lla,a).
tail(lla,llempty).

head(llbc,b).
tail(llbc,llc).

head(llac,a).
tail(llac,llc).

head(lle, e).

empty(llempty,llempty).
